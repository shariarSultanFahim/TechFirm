# Blockers & Assumptions

## Active Blockers
*None currently.*

## Key Working Assumptions
- Local Figma MCP server (`http://127.0.0.1:3845/mcp`) is actively queried for layout, colors, and typography.
- MongoDB connection is configured via `.env` in `apps/api`.
- Admin credentials for default seed: `admin@techfirm.com` / `Admin123!`.
